server.port=8080

# PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/simgan_db
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# CORS
spring.web.cors.allowed-origins=http://localhost:5173,http://localhost:3000

# ===== Planet Labs API =====
# Pon tu API Key aqui (Education License)
planet.api.key=PLAK796ad7f70ae441f3b58f48afacb24180
planet.api.base-url=https://api.planet.com/data/v1

# Item type: PSScene (PlanetScope 3-5m daily)
planet.item-type=PSScene

# Asset type: ortho_analytic_4b_sr (Surface Reflectance, 4-band)
planet.asset-type=ortho_analytic_4b_sr

# ===== NDVI Config =====
# Umbral minimo de NDVI para alerta (pasto degradado)
ndvi.alert.threshold=0.3

# Umbral optimo de NDVI (pasto saludable, listo para pastoreo)
ndvi.optimal.threshold=0.6

# Dias de descanso recomendados cuando NDVI < threshold
ndvi.rest.days=21

# ===== Seed Data =====
# Generar datos demo al iniciar (true/false)
ndvi.seed.enabled=true

# Meses de datos historicos a generar
ndvi.seed.months=6

# Directorio temporal para descargas de GeoTIFF
planet.download.dir=${java.io.tmpdir}/simgan-planet

# ===== Sentinel-2 / Copernicus Data Space (GRATUITO) =====
# La búsqueda de escenas es PÚBLICA y no requiere autenticación.
# Para DESCARGAR imágenes necesitas credenciales OAuth2:
#
# 1. Cuenta Copernicus: https://dataspace.copernicus.eu (ya la tienes)
# 2. El sistema usa tu email/password para obtener un token OAuth2 automáticamente
# 3. NO es una API key fija como Planet Labs
#
# Pon tu email y password de Copernicus Data Space aquí:
copernicus.username=sebastianlalo123@gmail.com
copernicus.password=cANCHIS889911.

# Alternativamente, si usas Sentinel Hub (sentinelhub.com):
# 1. Ve a Dashboard → User Settings → OAuth Clients
# 2. Crea un nuevo OAuth Client
# 3. Copia el client_id y client_secret
sentinel.hub.client.id=sh-1754b371-e044-4acd-9384-99df745e8dd5
sentinel.hub.client.secret=JDu9ygGeqrKshSLaWH3Y8pLP9p64gMaN
