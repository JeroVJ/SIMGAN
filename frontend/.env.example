# Base URL of the backend API (including /api context path)
# Leave empty in development — vite.config.js proxies /api to localhost:8080
# In production (Vercel), set to your backend public URL, e.g.:
#   https://simgan-api.onrender.com/api
VITE_API_URL=
